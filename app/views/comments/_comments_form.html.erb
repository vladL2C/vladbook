    <div class="field">
      <div class="control">
        <%= form_for(:comment, url: comments_path) do |f| %>
        <%= f.text_area :content, size: "50x4", placeholder: "Write something for #{@user.name}..." %> 
        <%= f.submit "Comment", class: "button is-info is-small" %>
        <% end %>
      </div>
    </div>